<main class="uk-width-expand uk-position-relative">
  <p id="notice"><%= notice %></p>

  <h1>Orders</h1>

  <table>
    <thead>
    <tr>
      <th>Order ID</th>
      <th>Category</th>
      <th>Brand</th>
      <th>Amount</th>
      <th>Description</th>
      <th>Processing Time</th>
      <th colspan="3"></th>
    </tr>
    </thead>

    <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.id %></td>
<!--        <td><%#= order.name %></td>-->
<!--        <td><%#= order.name %></td>-->
<!--        <td><%#= order.name %></td>-->
<!--        <td><%#= order.name %></td>-->
<!--        <td><%#= order.name %></td>-->
        <td><%= link_to 'Show', users_customer_order_path(order) %></td>
        <td><%= link_to 'Edit', edit_users_customers_order_path(order) %></td>
        <td><%= link_to 'Destroy', users_customer_order_path(order), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Order', new_users_customers_order_path %>

</main>