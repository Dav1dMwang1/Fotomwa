<main class="uk-width-expand uk-position-relative">
  <h1>Editing Brand</h1>

  <%= render 'form', brand: @brand %>

  <%= link_to 'Show', admin_brand_path(@brand) %> |
  <%= link_to 'Back', admin_brand_path %>

</main>